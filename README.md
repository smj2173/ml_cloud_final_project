Model for training and evaluating model against ImageNet and DAmageNet datasets. A subset of ImageNet/DAmageNet is used, looking at 10 image classes (dog, fish, golf ball etc).

ResNet-18 used for training. 
